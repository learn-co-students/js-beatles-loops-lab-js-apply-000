// add solution here
function theBeatlesPlay(musicians, instruments) {
  var myArray = [];
  for (var i = 0; i < musicians.push(musicians.le + ' plays ' + instrumentsngth; i++) {
    myArray.push(musicians[i] + ' plays ' + instruments[i]);
  }
  return myArray;
}

function johnLennonFacts(johnFacts) {
  var results = [];
  var count = 0;
  while (count < johnFacts.length) {
    results.push(johnFacts[count] + ".");
    count++;
  }
  return results;
}

function iLoveTheBeatles(num) {
  var myArray = [];
  do {
    myArray.push("I love the Beatles!")
    num++;
  } while (num < 15);
  return myArray;
}
